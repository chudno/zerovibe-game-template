---
name: archetype-wheel
description: Рецепт доводки кита «Розыгрыш» (wheel) — колесо / скретч-карта / коробка с призом по весам из content/wheel.json; как собрать призы и веса под бриф, выбрать подачу (params.presentation), что крутить, почему веса подтверждает человек, грабли (одноразовые коды, «Ещё раз», нулевой вес) и как объяснить заказчику. Используй, когда config.archetype = "wheel" или человек описывает «колесо фортуны», «розыгрыш промокода», «скретч», «открой коробку».
---

# Розыгрыш (wheel): доводка кита

Файл `game/kits/wheel/kit.js` — код не трогаем. Призы — `content/wheel.json`,
подача и темп — `game/config.js` → `params`. Формат — `game/kits/wheel/README.md`.

## Что собрать под бриф

1. **Призы (2–12).** `id`, `title` ≤ 24 символов (сектор узкий: «Скидка 10%»,
   «Доставка 0 ₽»), `code`, `text` (что делать с кодом), `color`.
2. **Веса.** `weight` — шанс: вес / сумма весов. **Веса подтверждает человек
   явно** — это вероятность выигрыша в акции бренда, юридически значимая
   величина. Предложи таблицу «приз — шанс в %» и спроси: «Так и запускаем?».
   Сам веса не выдумывай «на глаз».
3. **Сектор для красоты** — `weight: 0` («Повезёт завтра»): виден, не выпадает.
   Не более одного-двух, иначе колесо выглядит обманом.
4. **Подача** — `presentation`: `wheel` (универсально), `scratch` (когда бренд
   про «стереть и узнать», карты лояльности), `lootbox` (подарки, распаковка).
   Спроси человека одним вопросом с тремя вариантами.

## Что крутить — `config.params`

`presentation`, `spinMs` 3200 (2500–4000: короче — не читается, дольше —
скучно), `scratchPercent` 55, `replay` false.

## Одноразовые коды

Игра — публичная статическая страница: всё, что в `content/wheel.json`,
видно любому. Значит в игре лежат **общие** коды акции. Одноразовые коды и
лимит «одна попытка на человека» выдаёт сайт бренда по событию `finish`
(`outcome` = id приза, `prize.code`) — скилл `embed`. Скажи это заказчику до
того, как он попросит «уникальный код каждому».

## Грабли

- **`replay: true` в розыгрыше** — игрок крутит до нужного приза. Оставь false,
  если бриф прямо не просит «крутить сколько угодно».
- **Сумма весов 0 или один ненулевой приз** — валидатор красный.
- **Длинный `title`** — не влезает в сектор; подробности пиши в `text`, он
  показывается на карточке результата.
- **Колесо картинкой** — не делай: вращение мылит пиксель-арт и текст. Кит
  бежит подсветкой по неподвижным секторам — это осознанно.
- Коробке можно дать картинки `items.box`/`items.boxOpen` одного размера;
  колесо и скретч картинок не требуют.

## Как объяснить заказчику

«Человек делает одно действие — крутит колесо (стирает карту, открывает
коробку) — и получает приз с промокодом. Шансы вы задаёте сами, таблицей;
я её покажу перед запуском. Коды в игре общие для всех; если нужны
персональные — их выдаёт ваш сайт в момент выигрыша, это делается на вашей
стороне».

## Готово, когда

Розыгрыш прошёл во всех трёх подачах (или в выбранной — дважды), приз и код
показаны, веса подтверждены человеком письменно в чате, `node --test tests
game` зелёный. Дальше — `ready-checklist`.
