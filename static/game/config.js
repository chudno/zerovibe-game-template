// Настройки игры. Здесь меняется всё, что зависит от заказа: название,
// архетип и фирменные цвета. Логика китов и оболочки сюда не лезет.
window.ZV_GAME = {
  // Заголовок на первом экране.
  title: "Название игры",

  // Архетип: "runner" | "catch" | "quiz". Выбирает кит в game/kits/.
  archetype: "runner",

  // Оформление под заказчика.
  brand: {
    name: "Бренд",
    primary: "#4f7cff",    // акцент: кнопки, счёт
    secondary: "#ffd23f",  // второй акцент: цели, награды
    logoUrl: ""            // ссылка на файл проекта; пусто — логотипа нет
  },

  // Встраивание: ?embed=1 в iframe — оболочка прячет «Во весь экран».
  embed: new URLSearchParams(location.search).get("embed") === "1"
};
