// Настройки игры. Здесь меняется всё, что зависит от заказа: название,
// архетип, фирменные цвета и картинки. Логика китов и оболочки сюда не лезет.
window.ZV_GAME = {
  // Заголовок на первом экране.
  title: "Название игры",

  // Архетип: "runner" | "catch" | "quiz" | "persona" | "wheel" | "platformer" |
  // "novel" | "quest". Выбирает кит в game/kits/. Описание и параметры —
  // game/kits/<archetype>/README.md.
  archetype: "runner",

  // Баланс кита: ключи и значения по умолчанию — в README кита (и в его
  // DEFAULTS). Пусто — дефолты. Неизвестный ключ или чужой тип — предупреждение
  // в консоли, игра идёт на дефолте. Пример для runner: { speedMax: 380 }.
  params: {},

  // Приз за победу — карточка на экране результата; нужен только
  // брендированной игре (скилл branding). Пустой title — карточки нет.
  // code показывается крупно и копируется кнопкой; url — «Забрать».
  // Киты «какой ты» и «розыгрыш» несут призы в своём content/*.json.
  prize: { title: "", code: "", text: "", button: "", url: "" },

  // Тема игры: подпись над названием на первом экране и два акцента.
  theme: {
    name: "",              // подпись над названием (студия, серия, бренд); пусто — без неё
    primary: "#4f7cff",    // акцент: кнопки, счёт
    secondary: "#ffd23f",  // второй акцент: цели, награды
    logoUrl: ""            // PNG с прозрачным фоном из файлов проекта: на «Играть» вместо name
                           // (до 240×80) и мелко на «Результат»; пусто — логотипа нет
  },

  // Тексты, уровни и сюжет (вопросы, типы, призы розыгрыша, карты платформера,
  // граф новеллы и квеста) — не здесь, а в content/*.json: quiz, persona,
  // wheel, levels, novel, quest. Формат — README кита.

  // Картинки. Пусто — кит рисует серые заглушки и играется как есть.
  // Числа берутся ИЗ ОТВЕТА asset_generate (frame_width/frame_height/cols),
  // не на глаз. Все спрайты ставятся масштабом 1: пиксель-арт не масштабируют.
  assets: {
    // Герой: лист кадров. frames — сколько кадров в анимации бега,
    // fps 8–12 для персонажа. Одна картинка без анимации — frames: 1.
    hero: { url: "", frameWidth: 0, frameHeight: 0, frames: 0, fps: 10 },

    // Предметы. Ключи разные у китов: runner — block/blockTall/ground,
    // catch — basket/good/bad, wheel (lootbox) — box/boxOpen,
    // platformer — ground/coin/spike/exit, quest — иконки предметов по
    // ключу icon из content/quest.json. Каждый: { url: "" }.
    items: {},

    // Фон. tile: true — только если стороны текстуры 64/128/256 (иначе
    // WebGL сглаживает её вопреки pixelArt); иначе обычная картинка.
    background: { url: "", tile: false },

    // Новелла и квест: фоны и портреты по ключам из content/novel.json или
    // content/quest.json (node.bg, node.portrait). Каждый: { url: "" }. Пусто — заглушки.
    backgrounds: {},
    portraits: {}
  },

  // Встраивание: ?embed=1 в iframe — оболочка прячет «Во весь экран».
  embed: new URLSearchParams(location.search).get("embed") === "1"
};
